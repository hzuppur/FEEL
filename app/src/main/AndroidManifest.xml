<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.feel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.addFeeling.FeelPositivityActivity" />
        <activity android:name=".ui.addFeeling.FeelIntensityActivity" />
        <activity android:name=".ui.addFeeling.FeelBodyActivity" />
        <activity android:name=".ui.addFeeling.FeelTriggerActivity" />
        <activity android:name=".ui.addFeeling.FeelEmotionActivity" />
        <activity android:name=".ui.addFeeling.FeelWhenActivity" />
        <activity android:name=".ui.addFeeling.FeelReactedActivity" />
        <activity android:name=".ui.addFeeling.FeelUnderstandOrBetter" />
        <activity android:name=".FeelBetterActivity" />
        <activity android:name=".ui.exercises.Exercise1" />
        <activity android:name=".UnderstandActivity" />
    </application>

</manifest>